#include "scan.h"

#include <iostream>

scan::scan() {};

scan::~scan() {};

common::Tokens scan::operator() (const common::Words& words)
{
    common::TokenLine tokenLine;
    std::string line;

    return this->tokens;
}
